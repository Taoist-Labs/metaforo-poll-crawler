{
  "name": "metaforo-poll-crawler",
  "version": "1.0.0",
  "description": "A crawler to fetch poll related info from metaforo. Currently, it's dedicated for SeeDAO.",
  "main": "index.js",
  "scripts": {
    "start:dev": "npx nodemon",
    "start": "npm run build && node build/index.js",
    "build": "rimraf ./build && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.8.10",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@types/crawler": "^1.2.4",
    "@types/papaparse": "^5.3.10",
    "axios": "^1.6.0",
    "crawler": "^1.4.0",
    "csv-stringify": "^6.4.4",
    "papaparse": "^5.4.1"
  }
}
